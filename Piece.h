#pragma once#include <iostream>#include "Position.h"#include <vector>using namespace std;class Piece{protected:	Position position;	bool fWhite;	int nMoves;	int lastMove;	vector<string> moves;public:	Piece(int col, int row, bool white);	void assign(Position position);	void assign(string piece);	bool isWhite();	bool isMove();	int getNMoves();	Position getPosition();	bool justMoved();	char getLetter();	//void display(gout);	//int getMoves(board);};